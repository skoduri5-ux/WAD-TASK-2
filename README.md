# WAD-TASK-2
Assignment Points:

1. Apply inline, internal, and external CSS to different elements and observe how style precedence works.

2. Use basic CSS selectors (element, class, and ID) to style headings, paragraphs, and sections.

3. Style text using font properties and apply suitable text alignment for headings and content.

4. Create a list (ordered or unordered) and apply list and colour properties to improve readability.

5. Add a background colour or background image to one section of the page using CSS3.
